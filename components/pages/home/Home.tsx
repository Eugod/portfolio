import { Header } from "../../layouts/Header"
import styles from "./home.module.css"

export const HomePage = () => {
    return (
        <main>
            <Header />
            <p>Home</p>
        </main>
    )
}